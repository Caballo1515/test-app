import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
    theme:{
        primary: '#febf7c', 
        success: '#24bf77', 
        error: '#f90000'
    }
});
